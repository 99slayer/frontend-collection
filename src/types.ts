export interface ThemeInterface {
	name: string;
	themeMain: string;
}

export interface MessageInterface {
	userId: number;
	username: string;
	timestamp: string;
	text: string;
}

export interface CarouselInterface {
	title: string;
	text: string;
}
